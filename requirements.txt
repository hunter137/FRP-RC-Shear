# ── Core (required) ──────────────────────────────────────────────────
numpy>=1.21
pandas>=1.3
scipy>=1.7
scikit-learn>=1.0
joblib>=1.1
matplotlib>=3.5
PyQt5>=5.15
packaging>=21.0
openpyxl>=3.0
xlrd>=2.0

# ── ML backends (recommended) ────────────────────────────────────────
xgboost>=1.5
lightgbm>=3.3
# catboost>=1.0          # optional — large install

# ── Hyperparameter optimisation (recommended) ────────────────────────
optuna>=3.0

# ── Model interpretability (optional) ────────────────────────────────
shap>=0.41

# ── Multi-objective optimisation (optional) ──────────────────────────
pymoo>=0.6

# ── Deep learning models (optional) ──────────────────────────────────
# torch>=1.10            # required only for 1D-CNN model
